name: '[DEPRECATED] Deploy 3D Simulator (superseded by deploy-pages.yml)'

# DEPRECATION NOTICE (2026-04-20)
# ------------------------------------------------------------------------------
# This workflow is retained only to preserve history. All GitHub Pages
# deployments are now handled by .github/workflows/deploy-pages.yml on the
# `main` branch, publishing the docs/ folder (and syncing root-level
# simulator files into docs/ at build time).
#
# The previous configuration (branch=master, path=visualization/) conflicted
# with the current setup and is disabled. Do not re-enable without
# coordinating with deploy-pages.yml to avoid a deployment race.
# ------------------------------------------------------------------------------

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "This workflow is deprecated. See .github/workflows/deploy-pages.yml"
          echo "Triggered manually; no deployment will occur."
