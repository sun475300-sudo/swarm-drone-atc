"""챗봇 응답 엔진 기본 인터페이스."""

from __future__ import annotations

from abc import ABC, abstractmethod
from dataclasses import dataclass, field


@dataclass
class ChatResponse:
    """챗봇 응답 데이터."""

    answer: str
    confidence: float  # 0.0 ~ 1.0
    category: str
    entry_id: str
    law_basis: str
    related_ids: list[str] = field(default_factory=list)
    source: str = "rule"  # "rule" or "llm"
    needs_escalation: bool = False
    escalation_reason: str = ""


class BaseEngine(ABC):
    """응답 엔진 추상 기본 클래스."""

    @abstractmethod
    def query(self, user_input: str) -> ChatResponse | None:
        """사용자 입력에 대한 응답을 반환한다."""
        ...

    @abstractmethod
    def get_categories(self) -> list[dict]:
        """사용 가능한 카테고리 목록을 반환한다."""
        ...

    @abstractmethod
    def get_entry_by_id(self, entry_id: str) -> dict | None:
        """ID로 지식베이스 항목을 조회한다."""
        ...


# 시스템 프롬프트 (향후 LLM 엔진에서 사용)
SYSTEM_PROMPT = """너는 보세전시장 민원응대 전문 챗봇이다.

[역할]
- 보세전시장 관련 민원에 대해 이해하기 쉬운 한국어로 답변한다.
- 답변은 반드시 법제처 국가법령정보센터의 법령/행정규칙과 관세청 홈페이지 자료를 우선 참고한다.
- 법적 근거가 있으면 답변 끝에 "근거" 항목으로 법령명과 조문/고시명을 정리한다.
- 법령에 명확히 없는 내용은 추정하지 말고, "확인 필요"라고 말한다.
- 개별 사실관계에 따라 결론이 달라질 수 있으면 그 점을 분명히 밝힌다.
- 유권해석, 처분 예고, 불복절차 판단처럼 법적 책임이 큰 사안은 관할 세관 또는 관세청 담당부서 확인이 필요하다고 안내한다.

[답변 우선순위]
1. 법제처 국가법령정보센터의 현행 법령
2. 법제처 국가법령정보센터의 관세청 고시
3. 관세청 공식 홈페이지 및 고객지원센터 FAQ
4. 위 자료들로도 부족할 때만 일반 설명

[답변 원칙]
- 먼저 질문 의도를 분류한다:
  1) 제도 일반  2) 특허/운영  3) 반입/반출  4) 전시/사용
  5) 판매/직매  6) 견본품  7) 시식용 식품  8) 서류/신고
  9) 벌칙/제재  10) 담당기관 문의
- 질문이 모호해도 가능한 범위에서 가장 보수적으로 답한다.
- "될 수 있습니다/필요할 수 있습니다"와 "필요합니다/금지됩니다"를 엄격히 구분한다.
- 민원인이 바로 행동할 수 있도록 단계형으로 답변한다.
- 전문용어는 쉬운 말로 풀어 설명한다.
- 법령 근거가 확인되지 않으면 "현재 확인한 공식 자료만으로는 단정하기 어렵습니다"라고 답한다.

[반드시 포함할 표현 규칙]
- 개별 사안이면: "구체적인 물품 성질, 반입 목적, 판매 여부, 행사 방식, 신고 내용에 따라 달라질 수 있습니다."
- 법적 최종판단이 아니면: "안내용 설명이며, 최종 처리는 관할 세관 확인이 필요합니다."

[금지사항]
- 근거 없이 단정하지 않는다.
- 타 기관 소관 사항을 관세청이 최종 판단하는 것처럼 답하지 않는다.
- 법령에 없는 서류를 임의로 만들어 안내하지 않는다.
- 고객의 사실관계를 모르는 상태에서 "무조건 가능/무조건 불가"라고 단정하지 않는다.

[예외 처리]
- 시식용 식품, 검역, 식약처 협업, 세관장확인 대상은 식약처 등 관계기관 요건도 함께 언급한다.
- 시스템 장애, UNI-PASS 입력오류, 전산 사용 문제는 기술지원센터 문의를 안내한다.
- 보세전시장 제도 자체, 특허/운영 기준은 관세청 보세산업과 또는 관할 세관 확인을 안내한다.
"""

# 답변 템플릿
ANSWER_TEMPLATE = """문의하신 내용은 [{category}]에 관한 사항입니다.

{answer}

근거:
{law_basis}

안내:
- 본 답변은 일반적인 안내용 설명이며, 구체적인 사실관계에 따라 달라질 수 있습니다.
- 최종 처리는 관할 세관 또는 해당 소관기관 확인이 필요합니다."""

# 에스컬레이션 키워드
ESCALATION_KEYWORDS = [
    "유권해석", "불복", "이의신청", "심사청구", "심판청구",
    "처분", "과징금", "벌금", "처벌", "고발",
    "UNI-PASS", "유니패스", "전산", "시스템 오류", "시스템 장애",
]

# 에스컬레이션 메시지
ESCALATION_MESSAGE = """해당 문의는 개별 사실관계에 대한 구체적 판단이 필요한 사안입니다.

아래 담당기관으로 직접 문의하시기 바랍니다:

1. 제도 일반 상담: 관세청 고객지원센터 (☎ 125)
2. 보세전시장 제도 소관: 관세청 통관국 보세산업과
3. 전자통관 시스템 문의: 관세청 전자통관 기술지원센터 (☎ 1544-1285)
4. 개별 사안: 관할 세관 보세담당 부서

안내:
- 유권해석, 처분 예고, 불복절차 판단 등 법적 책임이 큰 사안은 관할 세관 또는 관세청 담당부서의 확인이 필요합니다."""

# 확인 질문 목록
CLARIFICATION_QUESTIONS = [
    "물품이 외국물품인가요?",
    "행사 목적이 전시인가요, 판매인가요, 시식·증정인가요?",
    "행사 장소가 보세전시장 특허 대상인가요?",
    "물품을 행사 후 재반출할 예정인가요, 국내 판매할 예정인가요?",
    "식품·의료기기·검역대상 등 타법 요건이 있는 물품인가요?",
    "이미 관할 세관과 협의하셨나요?",
]

# 챗봇 소개 메시지
WELCOME_MESSAGE = (
    "안녕하세요. 저는 법제처 국가법령정보센터의 현행 법령과 관세청 공식 자료를 "
    "우선 참고하여 보세전시장 민원을 안내하는 챗봇입니다.\n\n"
    "다만 구체적인 사실관계에 따라 결론이 달라질 수 있어 "
    "최종 처리는 관할 세관 확인이 필요할 수 있습니다.\n\n"
    "아래 카테고리를 선택하거나 질문을 입력해 주세요."
)
