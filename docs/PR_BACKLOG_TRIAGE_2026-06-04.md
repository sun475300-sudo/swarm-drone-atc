# PR 백로그 트리아지 — 2026-06-04

> 작성: 일일 점검 루틴 (claude/fervent-babbage-CxEfY)
> 목적: 열린 PR 25개의 정체 원인 진단 + 병합 순서·중복 정리 권고

## 1. 근본 원인 (P0)

머지 커밋 `a576460`이 8개 파일의 git conflict 마커를 **미해소 상태로 main에 커밋**했다.
`main` 자체가 SyntaxError/collection error 상태가 되어 **열린 PR 전부의 CI가 실패**한다
(GitHub은 PR을 `merge-with-main`으로 테스트하므로 PR 내용과 무관하게 실패).

- **PR #100** (`fix/main-merge-conflicts`)이 이를 복구한다 — 8개 파일을 CI 검증된 `c712bbd` 버전으로 환원, 충돌 마커 0, 비호환 테스트 정리.
- 2026-06-04 점검 시 PR #100 의 Security Audit ✅ / Simulator Smoke ✅ 통과, 메인 CI 진행 중.
- **조치: PR #100을 다른 모든 PR보다 먼저 병합**해야 main이 복구되고 나머지 PR의 CI가 정상화된다.

본 트리거 세션 검증: 로컬 트리 충돌 마커 **0**, `pytest --co` **3,831 테스트 수집**
(2개 에러는 선택적 의존성 `hypothesis`/`plotly` 부재 — CI 환경에는 설치됨, 코드 문제 아님).

## 2. 중복 PR 클러스터 (병합 전 정리 필요)

동일 기능을 여러 병렬 세션이 각자 PR로 올려 중복이 누적됐다.

| 로드맵 항목 | 중복 PR | 권고 |
|------------|---------|------|
| **P732** 대규모 CPA 충돌예측 공간해시 (B2) | #88, #83, #82, #80, #79, #77 (6개) | 가장 완성도 높은 1개만 유지, 나머지 close |
| **P730** UI i18n KO/EN (B5) | #76, #75, #81 (3개) | 1개 유지, 나머지 close (#81은 로드맵 갱신 포함 — 분리 검토) |
| **P734** 리플레이/멀티뷰 동기화 | #91, #89, #86 (3개) | 3개가 서로 다른 하위기능(스크러버/멀티뷰/멀티뷰싱크)일 수 있음 — diff 확인 후 통합 또는 순차 병합 |

## 3. 단일 PR (중복 없음 — main 복구 후 순차 검토)

| PR | 항목 |
|----|------|
| #84 | P731 공역 레이어 패널 통합 (O1) |
| #85 | P733 ws_bridge 데모↔실측 토글 |
| #92 | P735 해양 EO/IR 어댑터 |
| #87 | P711 React 프론트엔드 MVP |
| #97 | P729 대규모 모드 로터 InstancedMesh |
| #93, #96, #90 | Track B 논문 (P701/P707/P710 등) — 상호 의존, 병합 순서 주의 |
| #99, #94 | Track E 테스트 커버리지 |
| #95, #98, #73 | 최종 정리 / CONTRIBUTING·INDEX / CI·로드맵 |

## 4. 권고 병합 순서

1. **PR #100** 병합 → main 복구 (CI 녹색 확인 후)
2. 중복 클러스터 정리: P732(#77~#88) → 1개 선정, 5개 close / P730(#75,#76,#81) → 1개 선정
3. 단일 기능 PR을 의존성 낮은 순으로 순차 rebase·병합 (각 PR은 복구된 main 대비 CI 재실행 필요)
4. Track B 논문 PR(#90→#93→#96)은 상호 의존 순서 확인 후 병합

## 5. 미해결 결정 사항 (사용자 판단 필요)

- 중복 클러스터에서 **어느 PR을 정본으로 채택**할지 (diff 품질 비교 필요)
- PR #100을 **누가 병합**할지 (현재 별도 세션이 능동 처리 중 — 충돌 방지를 위해 단일 주체가 진행)
- 정체 PR 중 **로드맵에서 이미 완료 표기된 항목**(P729 등)의 PR을 닫을지 유지할지

---

*이 문서는 코드 변경 없이 백로그 상태만 기록한다. 실제 병합/close는 사용자 승인 후 진행.*
