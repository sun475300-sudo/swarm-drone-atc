# 일일 점검 2026-06-14 (20차) — 독립 재현 GREEN + 머지 병목 정밀 triage

> 점검자: 일일 점검 루틴 · main HEAD `32cdfd2` 기준 · 신규 컨테이너 독립 재현

## 1. 건강 상태 — ✅ GREEN

| 항목 | 결과 |
|---|---|
| 전체 회귀 (`pytest tests/ -n auto`) | **4,089 pass / 280 skip / 0 fail** (134.5s) |
| main 최신 커밋(`32cdfd2`) CI | CI·Security Audit·Pages·Canonical Hash 전부 **success** |
| 열린 이슈 | **0건** |
| 브랜치 ↔ main 동기 | 완전 동기 (rev-list 0/0) |

> 환경 함정(재현용): 신규 컨테이너에서 `blinker` RECORD 충돌은 `pip install --ignore-installed blinker`,
> `pytest-xdist`(pyproject `-n --dist loadfile` 요구)·`hypothesis` 추가 설치 필요.

## 2. ⚠️ 머지 병목 — owner 결정 필요 (PR 30건, 머지 0건)

루틴이 매 실행마다 신규 브랜치·PR을 생성하지만 머지되지 않아 적체 중. 19차(#303)에서
보고된 Phase 303 6중 중복은 owner가 #304 머지 + 중복 close 로 **해소 확인됨**. 그러나
신규 중복 클러스터가 누적됨. 모든 PR이 옛 main(`02d70b0`) 기준이라 순차 머지 시
CHANGELOG/README 충돌이 연쇄 발생함 → owner의 일괄 정리 권장.

### 2.1 중복 클러스터 (대표 1건만 머지 가능 — 설계가 상호 비호환)

| Phase | 중복 PR | 충돌/중복 성격 | 권장 |
|---|---|---|---|
| **307 사고보고** | #309·#305·#287·(#288·#295 콤보) | **설계 3종 비호환**: #309 `accident_report.py`(분리거리 로그 기반), #305 `incident_report.py`(이벤트유형+보고시한), #287 `incident_report.py`(시뮬 analytics.events 연동+CLI) — 파일명/ API 상충 | 1건 채택. 시뮬 연동성은 **#287**, 자기완결·풍부함은 **#305**. owner 선택 |
| **304 KC인증** | #307·#306 | 모듈명만 다른 쌍둥이(`kc_certification.py` vs `kc_radio_certification.py`), 동일 Phase 중복 | 1건 채택(대역/EIRP 테이블+custom bands 있는 **#306** 약간 우위) |
| **207 배지** | #281·#280 | **직접 충돌**: 동일 `tests/test_maturity_badge.py`·`extract_sdacs_api.py`·`maturity.svg` 동시 수정 | 1건만 머지 가능. CI smoke 게이트 연동된 **#281** 권장 |

### 2.2 단일(중복 없음) 기능 PR — 충돌만 풀면 머지 가능

#310(421 디스커버리)·#301(422 4D 볼륨)·#300(342 의료배송)·#299(226 WGS84 정밀)·
#298(322 스키마)·#293(449 갭모델)·#292(367 자가치유)·#291(401/406 EASA)·#290(286 Ablation)·
#289(445 신뢰구간)·#285(446 검정력)·#286(209/210 Deprecation)·#308(467 ICAO Annex13)

### 2.3 기타

- **dependabot 6건**: #274 matplotlib · #275 kaleido · #276 pydantic-core · #277 electron · #278 playwright · #279 electron-builder — 일괄 검토 권장.
- **#283**(핫루프 perf): non-draft이나 `mergeable_state=dirty`(충돌) + +56k라인(생성 HTML 포함) → rebase 필요.
- **#303**(19차 일일점검): stale·dirty. 본 20차로 superseded 가능.

## 3. 권장 조치 (owner)

1. 중복 클러스터별 대표 1건 선택 → rebase/충돌해소 후 머지, 나머지 close.
2. 단일 기능 PR은 rebase 후 순차 머지(또는 1개 통합 브랜치로 cherry-pick).
3. dependabot 6건 일괄 머지.
4. 향후 루틴이 **기존 열린 PR의 Phase를 먼저 확인**하고 중복 생성을 피하도록 운용.
