# Grafana 대시보드 자동 프로비저닝
apiVersion: 1

providers:
  - name: "SDACS Dashboards"
    orgId: 1
    folder: "SDACS"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
