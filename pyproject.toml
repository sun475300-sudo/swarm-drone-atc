[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "swarm-drone-atc"
version = "0.1.0"
description = "군집드론 공역통제 자동화 시스템 (SDACS)"
authors = [
    {name = "jangsunwoo", email = "sun475300@gmail.com"}
]
requires-python = ">=3.10"
readme = "README.md"
license = {text = "MIT"}
dependencies = [
    "numpy>=1.24",
    "simpy>=4.1",
    "scipy>=1.11",
    "pyyaml>=6.0",
    "dash>=2.17",
    "plotly>=5.20",
    "joblib>=1.3",
    "tqdm>=4.66",
    "pandas>=2.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.4,<9",
    "pytest-asyncio>=0.23",
    "pytest-cov>=7.0",
    "pytest-timeout>=2.3",
    "pytest-xdist>=3.5",
    "mypy>=1.10",
    "ruff>=0.4",
    "hypothesis>=6.100",
]
gpu = [
    "torch>=2.0",
]

[project.scripts]
sdacs = "main:main"

[project.urls]
Repository = "https://github.com/sun475300-sudo/swarm-drone-atc"
Demo = "https://heartfelt-axolotl-2f9af2.netlify.app"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --tb=short -n auto --dist loadfile --cov=src --cov=simulation --cov-report=term-missing --cov-fail-under=80"
markers = [
    "unit: 단위 테스트 (개별 함수/클래스)",
    "integration: 통합 테스트 (다중 모듈 상호작용)",
    "e2e: End-to-End 테스트 (전체 시뮬레이션 파이프라인)",
    "slow: 느린 테스트 (>10초)",
    "gpu: GPU 가속 테스트",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-dir]
"" = "src"

[tool.mypy]
python_version = "3.10"
warn_return_any = false
warn_unused_configs = true
disallow_untyped_defs = false
ignore_missing_imports = true
no_implicit_optional = true
exclude = ["tests/", "archive/", "visualization/"]

[[tool.mypy.overrides]]
module = ["simulation.*"]
ignore_errors = true

# 실기 통합(Phase 691-700) 완료 후 ignore 제거 예정
[[tool.mypy.overrides]]
module = ["src.hardware.*"]
ignore_errors = true

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "C4", "SIM"]
ignore = ["E501", "B008"]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]
"archive/**" = ["F401", "F811", "F841", "F821", "E402", "E701", "E702", "B007", "N806", "UP006", "UP035", "UP045", "I001", "E999"]
"visualization/**" = ["E402", "F401"]
"tests/**" = ["N806", "B007", "F841"]
