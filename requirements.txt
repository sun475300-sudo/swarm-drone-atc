# Core simulation
numpy>=2.0
simpy>=4.1
pandas>=2.2
scipy>=1.13
pyyaml>=6.0
joblib>=1.4
tqdm>=4.66

# Visualization
matplotlib>=3.10.9
# plotly 6.x 직렬화(bdata)는 dash 2.x 내장 plotly.js가 못 읽음 — dash>=3 필수
plotly>=6.0
seaborn>=0.13
dash>=3.0

# API (P711/P712)
fastapi>=0.139.0
# 보안: starlette CVE-2026-54282(낮음)·CVE-2026-54283(높음) 해소 하한 — fastapi transitive 이나 명시 가드
starlette>=1.3.1
uvicorn[standard]>=0.49.0
pydantic>=2.5
httpx>=0.27

# Dev
# pytest 9.0.3+ fixes the tmpdir handling advisory tracked by Dependabot.
pytest>=9.0.3,<10
pytest-cov>=7.0
pytest-asyncio>=1.4
# pyproject addopts 가 -n auto --dist loadfile 을 강제 — xdist 없으면 pytest 실행 자체가 불가
pytest-xdist>=3.5
# Property-based testing (A1-05). APF 단조성 / cutoff / clamp 불변성 검증.
hypothesis>=6.100
