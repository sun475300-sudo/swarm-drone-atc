# Core simulation
numpy>=2.0
simpy>=4.1
pandas>=2.2
scipy>=1.13
pyyaml>=6.0
joblib>=1.4
tqdm>=4.66

# Visualization
matplotlib>=3.9
plotly>=5.22
seaborn>=0.13
dash>=2.17

# P711/P712 FastAPI 백엔드 + 인증
fastapi>=0.110
uvicorn[standard]>=0.29
passlib>=1.7
python-multipart>=0.0.9

# Dev
# pytest 8.x line — pytest 9.0 메이저는 deprecated API 제거 + pytest-asyncio /
# pytest-cov 호환 매트릭스 미검증이라 별도 사이클로 미룸 (PR #20 참고).
pytest>=8.4,<9
pytest-cov>=5.0
pytest-asyncio>=0.23
# Property-based testing (A1-05). APF 단조성 / cutoff / clamp 불변성 검증.
hypothesis>=6.100
# P717 부하 테스트 — gevent monkey-patch 충돌로 CI에서 별도 설치 필요
# pip install ".[load-test]" 또는 pip install locust
# locust>=2.20  ← CI dev deps에서 제외; load-test extra에만 포함
